{
  "hooks": {
    "preflight": {
      "description": "Verify the Obsidian CLI is available and app-level readiness checks pass before any workflow runs.",
      "failure_reasons": {
        "app_not_running": "The Obsidian app is not running, so CLI handshake cannot complete.",
        "cli_registration_missing": "Obsidian CLI is not installed or no longer registered in the app settings."
      },
      "checks": [
        "run `obsidian version` before workflow execution",
        "if the readiness check fails, classify it into `app_not_running` or `cli_registration_missing` before suggesting recovery",
        "for `app_not_running`, instruct the user to open Obsidian, wait for vault indexing, then retry once",
        "for `cli_registration_missing`, instruct the user to verify Obsidian CLI registration and path visibility before retrying",
        "treat vault targeting as command validation: require explicit `vault=<name>` or `vault=<id>` when the current working directory is not an Obsidian vault"
      ],
      "retry_policy": {
        "max_guided_retries": 1,
        "notes": [
          "perform one guided retry after presenting reason-specific recovery",
          "if retry still fails, stop automation and ask for explicit user direction"
        ]
      }
    },
    "before_write": {
      "description": "Validate note writes before creating or modifying Markdown files through the Obsidian CLI.",
      "checks": [
        "confirm whether the target is a new note, an existing note, or the daily note",
        "prefer exact `path=<path>` targeting when a note name could be ambiguous",
        "validate that generated content is already clean Obsidian Markdown before calling `create`, `append`, or `prepend`",
        "prefer `property:set` for structured metadata when supported, otherwise embed frontmatter in the content body"
      ]
    },
    "after_write": {
      "description": "Perform a lightweight verification pass after a write operation completes.",
      "checks": [
        "re-read the target note when possible to confirm the write landed",
        "confirm that important properties or frontmatter fields are present",
        "flag suspicious outcomes such as an empty note, duplicated heading, or missing related-note section"
      ]
    },
    "guard_destructive": {
      "description": "Prevent destructive or high-risk file operations from running silently.",
      "protected_commands": [
        "obsidian delete",
        "obsidian move",
        "obsidian rename",
        "obsidian property:remove",
        "obsidian history:restore"
      ],
      "policy": [
        "do not execute destructive commands automatically during note capture, research, or backlink workflows",
        "require explicit user confirmation before any destructive file or property mutation",
        "prefer additive edits such as `append`, `prepend`, and `property:set` over destructive rewrites"
      ]
    }
  }
}
